Bootstrap: docker
From: continuumio/miniconda3
   
%files
    /home/fall/MONSDA/envs/scyphy.yaml /opt/envs/
   
%environment
   
%post
    ls -alrt /opt/envs
    chmod -R +x /opt/envs/scyphy.yaml
    
    ENV_NAME=scyphy
    echo ". /opt/conda/etc/profile.d/conda.sh" >> $APPTAINER_ENVIRONMENT
    echo "conda activate $ENV_NAME" >> $APPTAINER_ENVIRONMENT
   
    . /opt/conda/etc/profile.d/conda.sh
    conda env create -f /opt/envs/scyphy.yaml -p /opt/conda/envs/$ENV_NAME
    conda clean --all
   
%runscript
    exec "$@"
