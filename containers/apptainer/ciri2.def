Bootstrap: docker
From: continuumio/miniconda3
   
%files
    ${HOME}/MONSDA/envs/ciri2.yaml /opt/envs/
   
%environment
   
%post
    ls -alrt /opt/envs
    chmod -R +x /opt/envs/ciri2.yaml
    
    ENV_NAME=ciri2
    echo ". /opt/conda/etc/profile.d/conda.sh" >> $APPTAINER_ENVIRONMENT
    echo "conda activate $ENV_NAME" >> $APPTAINER_ENVIRONMENT
   
    . /opt/conda/etc/profile.d/conda.sh
    conda env create -f /opt/envs/ciri2.yaml -p /opt/conda/envs/$ENV_NAME
    conda clean --all
   
%runscript
    exec "$@"
