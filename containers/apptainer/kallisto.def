Bootstrap: docker
From: continuumio/miniconda3
   
%files
    ${HOME}/MONSDA/envs/kallisto.yaml /opt/envs/
   
%environment
   
%post
    ls -alrt /opt/envs
    chmod -R +x /opt/envs/kallisto.yaml
    
    ENV_NAME=kallisto
    echo ". /opt/conda/etc/profile.d/conda.sh" >> $APPTAINER_ENVIRONMENT
    echo "conda activate $ENV_NAME" >> $APPTAINER_ENVIRONMENT
   
    . /opt/conda/etc/profile.d/conda.sh
    conda env create -f /opt/envs/kallisto.yaml -p /opt/conda/envs/$ENV_NAME
    conda clean --all
   
%runscript
    exec "$@"
