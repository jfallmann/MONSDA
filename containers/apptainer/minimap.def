Bootstrap: docker
From: continuumio/miniconda3
   
%files
    /home/fall/MONSDA/envs/minimap.yaml /opt/envs/
    ${HOME}/MONSDA/scripts /opt/MONSDA/

%environment
   
%post
    ls -alrt /opt/envs
    chmod -R +x /opt/envs/minimap.yaml
    
    ENV_NAME=minimap
    echo ". /opt/conda/etc/profile.d/conda.sh" >> $APPTAINER_ENVIRONMENT
    echo "conda activate $ENV_NAME" >> $APPTAINER_ENVIRONMENT
   
    . /opt/conda/etc/profile.d/conda.sh
    conda env create -f /opt/envs/minimap.yaml -p /opt/conda/envs/$ENV_NAME
    conda clean --all
   
%runscript
    exec "$@"
